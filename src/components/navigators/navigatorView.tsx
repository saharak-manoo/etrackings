import React from 'react';
import NavigatorStack from './navigator';
import TabNavigation from './tabNavigation';

const NavigatorView: React.FC = props => {
  return <NavigatorStack />;
};

export default NavigatorView;
